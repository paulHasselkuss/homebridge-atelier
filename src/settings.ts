/**
 * The name of the platform users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'AtelierRs232HomebridgePlugin';

/**
 * Must match the name of the plugin as defined the package.json
 */
export const PLUGIN_NAME = 'homebridge-atelier-rs232';